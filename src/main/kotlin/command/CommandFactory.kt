package command

class CommandFactory {

    /*fun create(type: command.CommandsType, command: command.Command): command.Command {
        return when(type) {
            command.CommandsType.PRIVATE -> Private(command.name, command.description, command.subCommands)
            command.CommandsType.MUSIC -> command
        }
    }*/
}